name: CI

on:
  push:
    branches: [master, develop]
  pull_request:
    branches: [master, develop]

jobs:
  quality-and-tests:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        node-version: [18.x, 20.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint code (Node 20.x only)
        if: matrix.node-version == '20.x'
        run: npm run lint

      - name: Run unit tests
        run: npm run test:unit

      - name: Run integration tests
        run: npm run test:integration

      - name: Setup Playwright (Node 20.x only)
        if: matrix.node-version == '20.x'
        run: npx playwright install --with-deps firefox

      - name: Run E2E tests (Node 20.x only)
        if: matrix.node-version == '20.x'
        run: npm run test:e2e

      - name: Generate consolidated test report (Node 20.x only)
        if: matrix.node-version == '20.x' && always()
        run: node scripts/generate-test-report.js || true

      - name: Publish test summary (Node 20.x only)
        if: matrix.node-version == '20.x' && always()
        run: |
          if [ -f test-results/summary.md ]; then
            cat test-results/summary.md >> "$GITHUB_STEP_SUMMARY"
          else
            echo "No test summary report was generated." >> "$GITHUB_STEP_SUMMARY"
          fi
